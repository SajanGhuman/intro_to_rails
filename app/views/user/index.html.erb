<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>index</title>
</head>
<body class="bg-custom_blue">
    <div class="flex flex-col text-white w-[100vw] h-[100vh] justify-center items-center relative top-[-300px]">
    <h1 class="">Users Table</h1>
    <table class="">      
    <thead>
    <tr class="border-[1px] border-white">
    <th>ID</th>
    <th>Name</th>
    <th>User Name</th>
    </tr>
    </thead>
    <tbody>
    <% @users.each do |user| %>
        <tr class="border-[1px] border-white text-center">
        <td class="px-[50px]"><%= user.id %></td>
        <td class="px-[50px]"><%= user.name %></td>
        <td class="px-[50px]"><%= user.userName %></td>
        </tr>
        <% end %>
    </tbody>
    </table>
    </div>
    <div class="absolute left-[50%] top-[50%] translate-x-[-50%] translate-y-[50%] text-white">
    <%= paginate @users %> 
    </div>
</body>
</html>