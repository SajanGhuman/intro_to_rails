<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>index</title>
</head>
<body class="bg-custom_blue">
<nav class="absolute top-[20px] left-[50px] flex justify-center items-center my-4 z-10">
<ul class="flex space-x-4">
    <li class="text-[25px] font-bold"><a href="/" class="text-white hover:text-gray-300">Home</a></li>
    <li class="text-[25px] font-bold"><a href="/about" class="text-white hover:text-gray-300">About</a></li>
    <li class="text-[25px] font-bold"><a href="/animes" class="text-white hover:text-gray-300">Anime</a></li>
    <li class="text-[25px] font-bold"><a href="/users" class="text-white hover:text-gray-300">Users</a></li>
    <li class="text-[25px] font-bold"><a href="/comments" class="text-white hover:text-gray-300">Comments</a></li>
</ul>
</nav>
    <div class="flex flex-col text-white w-[100vw] h-[100vh] justify-center items-center relative top-[-200px]">
    <h1 class="">Users Table</h1>
    <table class="">      
    <thead>
    <tr class="border-[1px] border-white">
    <th>ID</th>
    <th>Profile</th>
    <th>Name</th>
    <th>User Name</th>
    </tr>
    </thead>
    <tbody>
    <% @users.each do |user| %>
        <tr class="border-[1px] border-white text-center">
        <td class="px-[50px]"><%= link_to "/user/#{user.id}" do %><%= user.id %><% end %></td>
        <td class="px-[50px]"><%= link_to "/user/#{user.id}" do %><img class="w-[50px] h-[50px]" src=<%= user.pimage %>/><% end %></td>
        <td class="px-[50px]"><%= link_to "/user/#{user.id}" do %><%= user.name %><% end %></td>
        <td class="px-[50px]"><%= link_to "/user/#{user.id}" do %><%= user.last_name %><% end %></td>
        </tr>
        <% end %>
    </tbody>
    </table>
    </div>
    <div class="absolute left-[50%] top-[80%] translate-x-[-50%] translate-y-[50%] text-white">
    <%= paginate @users %> 
    </div>
</body>
</html>